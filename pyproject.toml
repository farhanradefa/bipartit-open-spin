[project]
name = "bipartit-open-spin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "qutip>=5.3.1",
    "scipy>=1.18.1",
]

[project.scripts]
bipartit-open-spin = "bipartit_open_spin:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
